/**
 * Budi Ensemble Website Section Display Configuration
 * 
 * Set each section to `true` to display it on the website, 
 * or `false` to hide/disable it.
 */
export const SECTION_CONFIG = {
  hero: true,
  about: true,
  gallery: true, // Photo Gallery (ImageCarousel)
  artists: true,
  portfolio: true,
  map: true,
  contact: true,
  grillMe: false, // AI performance planning subpage
};
